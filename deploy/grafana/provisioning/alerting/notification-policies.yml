apiVersion: 1

policies:
  - orgId: 1
    receiver: Telegram
    group_by:
      - grafana_folder
      - alertname
      - severity
    group_wait: 10s
    group_interval: 1m
    repeat_interval: 30m